# -*- coding: utf-8 -*-
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')

part2 = """
## ГЛАВА 1А. СИСТЕМЫ РЕКОМЕНДАЦИЙ: ОБЗОР, АЛГОРИТМЫ И ПЕРСПЕКТИВЫ

### 1А.1. Понятие и классификация рекомендательных систем

Рекомендательная система (Recommendation System) — это информационная система, предназначенная для прогнозирования оценки или предпочтения пользователя по отношению к объекту (элементу каталога). В контексте музыкальных сервисов объектами рекомендаций выступают треки, артисты, альбомы и плейлисты. Рекомендательные системы являются критически важным компонентом современных интернет-сервисов, определяющим пользовательский опыт и в значительной степени влияющим на коммерческую успешность платформы.

Классификация рекомендательных систем основана на подходе к генерации рекомендаций и типе используемых данных:

1. Коллаборативная фильтрация (Collaborative Filtering, CF) — подход, основанный на анализе поведения группы пользователей. Ключевое предположение: пользователи, имевшие схожее поведение в прошлом, будут иметь схожие предпочтения в будущем. CF не требует информации о свойствах самих объектов — достаточно данных о взаимодействиях «пользователь-объект». Основным преимуществом данного подхода является способность выявлять неочевидные закономерности, недоступные при анализе только свойств объектов.

2. Контентная фильтрация (Content-Based Filtering, CBF) — подход, рекомендующий объекты на основе их атрибутов и предыдущих предпочтений пользователя. Для музыкальных рекомендаций атрибутами являются жанр, темп, энергичность, акустичность и другие характеристики трека. Преимуществом CBF является возможность рекомендовать новые объекты (cold start для объектов) и прозрачность рекомендаций.

3. Гибридные системы (Hybrid Systems) — комбинация коллаборативной и контентной фильтрации, преодолевающая ограничения каждого из подходов. Большинство коммерческих сервисов используют гибридные подходы, поскольку они обеспечивают наилучшее качество рекомендаций при различных сценариях использования.

4. Глубокое обучение (Deep Learning) — подход, использующий нейронные сети для моделирования сложных зависимостей в данных. Включает авторекуррентные сети (RNN), свёрточные сети (CNN), трансформеры (BERT) и графовые нейронные сети (GNN).

5. Контекстные системы (Context-Aware) — учитывают дополнительную информацию о контексте: время суток, местоположение, устройство, настроение пользователя. В музыкальных сервисах контекст играет особую роль — предпочтения существенно различаются в зависимости от времени дня, физической активности и других факторов.

6. Социальные рекомендации (Social Recommendations) — используют данные социальных графов: предпочтения друзей, совместные плейлисты, музыкальные сообщества.

### 1А.2. Коллаборативная фильтрация: математические основы

Коллаборативная фильтрация является наиболее распространённым подходом к построению рекомендательных систем. Рассмотрим математические основы различных вариантов CF.

Пользователь-пользователь CF (Memory-based). Пусть r_ui — известная оценка пользователя u объектом i. Предсказание новой оценки выполняется по формуле: r̂_ui = r̄_u + (Σ_{v∈N(u)} sim(u,v) × (r_vi - r̄_v)) / (Σ_{v∈N(u)} |sim(u,v)|), где r̄_u — средняя оценка пользователя u, N(u) — множество «соседей» (похожих пользователей), sim(u,v) — мера схожести пользователей u и v.

Меры схожести пользователей включают: косинусное сходство sim(u,v) = (r_u · r_v) / (||r_u|| × ||r_v||), которое измеряет угловое расстояние между векторами оценок; корреляцию Пирсона sim(u,v) = Σ_i((r_ui - r̄_u)(r_vi - r̄_v)) / (σ_u × σ_v), учитывающую смещение средних оценок пользователей; коэффициент Жаккарда sim(u,v) = |I_u ∩ I_v| / |I_u ∪ I_v|, где I_u — множество объектов, оценённых пользователем u, определяющий схожесть на основе пересечения интересов.

Item-based CF заменяет «соседей-пользователей» на «соседей-объекты»: r̂_ui = (Σ_{j∈N(i)} sim(i,j) × r_uj) / (Σ_{j∈N(i)} |sim(i,j)|). Преимущества item-based CF: стабильность (предпочтения объектов меняются медленнее, чем предпочтения пользователей), масштабируемость (предварительное вычисление попарных сходств объектов), интерпретируемость (можно объяснить рекомендацию через похожие объекты).

### 1А.3. Матричная факторизация: SVD, ALS, SGD

Матричная因子分解 (Matrix Factorization, MF) является обобщённым подходом к коллаборативной фильтрации. Ключевая идея: аппроксимация разреженной матрицы пользовательских оценок R произведением двух плотных матриц更低ого ранга: R ≈ P × Q^T, где P — матрица пользовательских факторов (m×k), Q — матрица предметных факторов (n×k), k — количество скрытых факторов.

Сингулярное разложение (SVD): любую матрицу A размерности m×n можно представить в виде A = U × Σ × V^T, где U — ортогональная матрица m×m, Σ — диагональная матрица сингулярных чисел, V — ортогональная матрица n×n. Приближение ранга k: A_k = U_k × Σ_k × V_k^T. Скрытые факторы k могут интерпретироваться как абстрактные характеристики (энергичность, позитивность и т.д.).

Stochastic Gradient Descent (SGD) минимизирует функцию потерь: L = Σ_{(u,i)∈Ω} (r_ui - p_u^T × q_i)^2 + λ(||p_u||^2 + ||q_i||^2), где Ω — множество известных оценок, λ — регуляризация. Обновление: p_u := p_u + lr × (e_ui × q_i - λ × p_u), q_i := q_i + lr × (e_ui × p_u - λ × q_i), где e_ui = r_ui - p_u^T × q_i.

Alternating Least Squares (ALS) фиксирует одну матрицу и оптимизирует другую аналитически. Преимущество: гарантированная сходимость, параллелизация (каждый пользователь/объект обрабатывается независимо).

### 1А.4. Контентная фильтрация

Контентная фильтрация строит модель предпочтений пользователя на основе свойств ранее понравившихся объектов. Для музыкальных треков основными атрибутами являются: Tempo (BPM), Energy (энергичность), Valence (позитивность), Danceability (танцевальность), Acousticness (акустичность), Speechiness (речевость), Instrumentalness (инструментальность), Liveness (живое исполнение), Loudness (громкость). Модель пользователя строится как вектор предпочтений, предсказание: r̂_ui = sim(p_u, f_i), где p_u — вектор пользователя, f_i — вектор признаков объекта.

### 1А.5. Нейросетевые модели для рекомендаций

Авторекуррентные сети (RNN/LSTM/GRU) моделируют последовательности прослушиваний. На вход подаётся последовательность ID треков, на выходе — предсказание следующего трека. Архитектура: Embedding → LSTM → Dense → Softmax. Преимущество: учёт порядка прослушиваний.

Трансформеры (BERT4Rec) адаптируют архитектуру BERT для рекомендаций. Маскирование случайных элементов последовательности и предсказание их по контексту. Преимущество: параллельная обработка, глобальный контекст.

Графовые нейронные сети (GNN) моделируют пользователей и объекты как узлы графа, взаимодействия — как рёбра. Message Passing позволяет агрегировать информацию о соседях. Преимущество: естественное моделирование социальных связей.

### 1А.6. Рекомендательные системы в коммерческих сервисах

Spotify использует гибридный подход: коллаборативная фильтрация на основе 200+ миллиардов событий, контентный анализ через 13 аудиохарактеристик, NLP-анализ текстов, контекстные рекомендации. Ключевые продукты: Discover Weekly (30 треков), Release Radar, Daily Mix.

Apple Music: поведенческий анализ + жанровые предпочтения + ручная курация + контекстные плейлисты (Activity playlists).

Яндекс.Музыка: нейросетевые модели + контекстные факторы (погода, время, активность), «Умный плейлист».

### 1А.7. Проблемы и ограничения существующих систем

Холодный старт (Cold Start) — проблема рекомендаций для новых пользователей/объектов. Решения: контентная фильтрация, запрос начальных предпочтений. Разреженность данных (Data Sparsity) — матрица содержит менее 1% заполненных элементов. Решения: матричная因子分解, нейросетевые модели. Фильтрующие пузыри (Filter Bubbles) — ограничение разнообразия контента. Серия (Serendipity) — отсутствие открытий нового. Объяснимость (Explainability) — сложность объяснения причин рекомендации.

### 1А.8. Будущие технологии в области рекомендаций

Генеративный AI (MusicLM, MusicGen, Udio) — персонализированная генерация музыки под конкретного пользователя. Мультимодальные модели — объединение аудио, текста (лирики), визуального контента (обложки, видео) и метаданных в единой модели.

Эмоциональный AI — анализ эмоционального состояния через биометрические данные (ЧСС, ЭЭГ) для адаптации рекомендаций к настроению. Интеграция с фитнес-трекерами и умными часами.

Графовые знания — построение графа знаний, связывающего артистов, жанры, эпохи, музыкальные движения, культурный контекст. Графовые нейронные сети используют эту структуру для более точных рекомендаций.

Федеративное обучение (Federated Learning) — обучение моделей на распределённых устройствах пользователей без централизованного сбора данных. Решает проблему приватности.

Кроссплатформенные рекомендации — объединение данных из различных сервисов (музыка, подкасты, аудиокниги, видео) для единого профиля предпочтений.

Квантовые вычисления — перспективное применение квантовых алгоритмов для ускорения матричной因子分解 и обработки графов.

### 1А.9. Сравнительный анализ алгоритмов

Сравнение 9 алгоритмов по 5 критериям показывает, что каждый алгоритм имеет свои сильные и слабые стороны: User-based CF (средняя точность, плохо для холодного старта, низкая сложность), Item-based CF (средняя, средняя масштабируемость), SVD (высокая, хорошая масштабируемость), ALS (высокая, отличная масштабируемость), Контентная (средняя, хорошо для холодного старта), RNN/LSTM (высокая, сложная реализация), Трансформеры (очень высокая, очень сложная), GNN (очень высокая, сложная), Гибридная (очень высокая, хорошая).

### 1А.10. Обоснование выбора SVD для разрабатываемой системы

Выбор SVD (SGD) обусловлен: баланс точности и простоты реализации, интерпретируемость скрытых факторов, инкрементальное обучение, доказанная эффективность (Netflix Prize), адаптация к размеру данных (k = min(20, min(users, items) - 1)). Гиперпараметры подобраны эмпирически: k=20, lr=0.005, λ=0.02, iterations=50.
"""

with open(r'C:\Users\USER\music-streaming\diploma\thesis_part2.txt', 'w', encoding='utf-8') as f:
    f.write(part2)

print(f"Part 2: {len(part2)} chars, {len(part2.split())} words")
